{
  "format": "CF_HTML",
  "origin": "synthetic",
  "description": "Plain HTML handed to the CF_HTML parser",
  "expect": "error",
  "expect_error_kind": "BadMagic",
  "notes": "CF_HTML has no signature; the Version: line is the closest thing. Requiring it is what stops bare markup - or any other text flavour routed to the wrong decoder - from parsing as a fragment with no context and no offsets."
}
