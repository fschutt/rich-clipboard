{
  "format": "CF_HDROP",
  "origin": "synthetic",
  "description": "Two absolute paths, fWide=1, pFiles=20, drop point (0,0)",
  "expect": "ok",
  "notes": "Hand-built to the DROPFILES layout in MS docs. The second path carries non-ASCII so a UTF-16 decode that silently drops the high byte is caught."
}
